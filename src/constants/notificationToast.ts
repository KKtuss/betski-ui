/** Matches `.notif-toast-stack` mobile rules in NotificationToastStack.css */
export const TOAST_MOBILE_BREAKPOINT = '(max-width: 640px)'

/** Horizontal drag distance before a swipe counts as dismiss (mobile). */
export const TOAST_SWIPE_DISMISS_OFFSET_PX = 72

/** Horizontal flick velocity that dismisses even below offset (mobile). */
export const TOAST_SWIPE_DISMISS_VELOCITY = 520
